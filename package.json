{
  "name": "storagestate-demo",
  "version": "1.0.0",
  "description": "Minimal Playwright project to understand storageState (auth reuse) on SauceDemo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.7.2"
  }
}
